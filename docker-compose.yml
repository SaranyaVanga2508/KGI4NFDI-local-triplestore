services:
  fuseki:
    image: stain/jena-fuseki:latest
    container_name: kgi4nfdi-fuseki
    ports:
      - "3030:3030"
    environment:
      - ADMIN_PASSWORD=admin
    volumes:
      - ./data/fuseki:/fuseki
    restart: unless-stopped